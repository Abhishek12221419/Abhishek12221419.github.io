export default function add(a, b) {
    return a + b;
}
export function subtract(a, b) {
    return a - b;
}

// export default add;   // We can export like this as well
// export {subtract};    // In a module we can use default only one time so here we have used normal exportation not using default so have to write like this